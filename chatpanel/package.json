{
  "name": "@ollama-chat/chatpanel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "copy:frontend": "cp dist/chat-panel.js ../frontend/public/chat-panel.js",
    "build:embed": "npm run build && npm run copy:frontend",
    "preview": "vite preview --port 5174"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.8"
  }
}
